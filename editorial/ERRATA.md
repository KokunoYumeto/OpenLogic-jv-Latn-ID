# Cathetan sumber lan notasi

The frozen English source remains byte-for-byte intact, and aligned translated files preserve its mathematical notation. The reader supplies the following explicit editorial clarifications. These concern source defects or scope, not claims established by the Javanese canon.

- OLP-0012, relations-as-sets.tex: the example writes K = L union I and H = G union I without first naming its identity relation I. Read I there as the diagonal identity relation on the natural numbers that the preceding paragraph describes.
- OLP-0018, trees.tex, definition of branches: the source writes z in X minus B, although the tree carrier is A and X is not bound. Read X as A in this definition. With that reading, maximality says that every point outside B is incomparable to at least one point in B.
- OLP-0016 and OLP-0019 use R+ for different locally defined closures: reflexive closure in the orders section, transitive closure in the operations section. Preserve that local convention; do not substitute one meaning in the other section.
- OLP-0005 states uniqueness/existence informally under extensionality. OLP-0010 explicitly qualifies uniqueness by existence and rejects unrestricted comprehension. Read the chapter through that qualification.

- OLP-0021, function-basics.tex: the square-root example calls the selected root positive although its natural-number domain includes zero. At zero, read this as the nonnegative principal square root. The predecessor/successors example names its input n and subsequently writes x; read that x as the same input n.
- OLP-0024, inverses.tex: the proposition that every injection has a left inverse omits the empty-domain exception. Its proof chooses a in A. For nonempty A the proof works; for A and B both empty the empty function is its own inverse; for A empty and B nonempty the empty injection has no left inverse B to A. Read the proposition with A nonempty or B empty. The right-inverse proof explicitly states its use of the Axiom of Choice in a footnote, which is retained.

The five Functions findings above were independently confirmed by shared source audit OLFUN-20260904: REVIEW.md SHA-256 bc183d34b6ac57cc00e2df76d00277cdd2fabee293d12beb142d2e27344f8d24 and FINDINGS.json SHA-256 eee57facbea44f65a19a816fb12cbc86be0b18e21dcffeeed943f52f7e332960. Frozen-source identities use archive/manifest/per-file hashes; the copied upstream directory has no nested Git repository. The target bodies now apply OLFUN-002 (nonnegative principal root) and OLFUN-003 (consistent x), OLFUN-001 adds nonempty A to the left-inverse proposition with the exact general condition in an adjacent note, OLFUN-004 calls the graph a relation between A and B contained in A times B, and OLFUN-005 explicitly distinguishes domain-only function restriction from the earlier two-coordinate relation restriction. Frozen English bytes remain unchanged.

The Size-of-Sets corrections below were independently confirmed by shared source audit OLSIZ-20260904: REVIEW.md SHA-256 26913176baacbda5ae8a47bcc82ccf0366b0763313e6eeeb45df59e64249a1f9 and FINDINGS.json SHA-256 9b6e836c8432eb75d331913983603796d6557da6ec3ad71b846b2a248374cd07. The localized bodies apply OLSIZ-001 through OLSIZ-010 and identify every change adjacent to its passage. A later proposed OLSIZ-011 finding was retracted after exact-byte inspection showed five `5c 5c 5c 68 6c 69 6e 65` sequences: each already encodes a row break followed by a genuine `\hline`, so no table-rule correction is applied. The final retraction artifact has SHA-256 b495d66165f3c7f0b32d61b0b106c402badcc6e4e6a130442734c8f883dd5adc; its tombstone has SHA-256 b1b9f8ad296bc8d718a17514cdb91c61a8c1ea537a1231604f862551406b35bf. Frozen English bytes remain unchanged.

These clarifications are machine-authored editorial additions. They are not silently substituted into the aligned source formulas.
- OLP-0029, enumerability.tex (OLSIZ-001): the last value row of the alternating-integer table stops at 3 beneath a header and formula row that continue through f(7). The localized table adds the required value -3 beneath f(7).
- OLP-0031, pairing.tex (continuing chapter, outside the first reader): the prose says the kth triangular number sums natural numbers strictly less than k, but gives k(k+1)/2. For the displayed sequence starting at zero and the correct pairing formula, read the sum through k, inclusive. The cofinite exercise omits the word subset before Nat in one sentence; the localized body states the complement in Nat of a finite subset there, as its explicit Nat-minus-A definition requires (OLSIZ-002).
- OLP-0031's final inverse-enumeration exercise uses the broad injective definition of pairing. Such an inverse is defined on the range, not necessarily on every natural number. Qualify by surjectivity to Nat, or re-enumerate the range; for an empty product use the empty-set exception. OLP-0032 explicitly distinguishes non-surjective encodings and partial inverses. The countable-union exercise can require a countable-choice principle in general; do not silently treat it as a theorem of bare ZF.
- OLP-0032, pairing-alt.tex: the opening prose calls the second pair (0,2), while its own table correctly shows (0,1). A later sentence repeats (2,m) where the progression requires (3,m) next. The localized body repairs both, with the shared audit identifying the second repair as OLSIZ-003.
- OLP-0033, non-enumerability.tex: the introductory claim that every enumerable set admits a surjection from the positive integers requires a nonempty set, as OLP-0029's definition explicitly allows the empty set separately. The two sets proved non-enumerable here are nonempty; their diagonal proofs are unaffected. Apply the same qualification to the introduction's list/surjection discussion.
- OLP-0034, reduction.tex: the characteristic-sequence definition names s_k without binding k; the localized body uses the single output name s throughout (OLSIZ-004). The final illustrative formula displays a finite string of n zeroes despite its infinite-sequence codomain; the localized counterexample uses n leading zeroes followed by an infinite tail of ones (OLSIZ-005).
- OLP-0035, equinumerous-sets.tex: both empty-set cases mistakenly write g(x)=y when arguing from the bijection f from A to B. The localized body uses f(x)=y in both places; the enumeration g is introduced only in the nonempty branch (OLSIZ-006).
- OLP-0036, comparing-size.tex: in the detailed Cantor proof, the sentence claiming a result for each x in the diagonal subset must range over every x in A. The localized body uses the whole domain, and the defining equivalence supplies both cases (OLSIZ-007). The result covers empty A too. The preceding general equivalence between non-enumerability and Nat being strictly smaller assumes that every infinite set under consideration contains a countably infinite subset (available with Choice); it should not be inferred in bare ZF for arbitrary sets.
- OLP-0038, enumerability-alt.tex: the opening sentence has a grammatical omission (finite set is). Read it as saying that a finite set can be specified by enumerating its elements. The finite initial domain in the formal definition is written {0,...,n}, which has n+1 elements; this does not conflict with the earlier example {0,...,n-1} for an n-element set, but the bound parameter changes convention.
- OLP-0039, non-enumerability-alt.tex: the prose defines s_n(m) as the nth digit of the mth string even though its notation and array use the mth digit of the nth string (OLSIZ-008). It also repeats the 1-to-0 instruction where the second change must be 0 to 1 (OLSIZ-009). The localized body repairs both. Its five original three-backslash row-break-plus-`\hline` sequences are retained after the proposed OLSIZ-011 finding was retracted.
- OLP-0040, reduction-alt.tex: the supposed Nat-indexed enumeration is displayed as N_1, N_2, ... rather than beginning at N_0, a harmless index shift. The characteristic sequence is called s_k without binding k; the localized body uses the single output name s throughout, as the defining membership formulas require (OLSIZ-010).
- OLP-0043, rationals.tex: the order explanation first correctly says that r is at most s when s minus r is nonnegative, but its following “i.e.” sentence reverses the difference to r minus s. The displayed definition again uses s minus r. The translated body follows that consistent order and records the repair adjacent to the formula.
- OLP-0044, reals.tex: the sentence about the rational set below square root of two contains a stray less-than sign immediately after the plural element token. It has no syntactic or mathematical role and is omitted in the translated body, with an adjacent correction note. An inactive commented-out footnote says that the symbol square root of two refers equally to positive and negative roots; under the standard convention used by the theorem it denotes the nonnegative principal root. The comment remains inactive and is translated for source alignment only.
- OLP-0045, cuts.tex: the first bullet of the completeness proof says that an upper bound guarantees that at least one cut belongs to S. That existence follows from the stated nonemptiness of S; the upper-bound assumption is used later to prove that the union is a proper subset of the rationals. The translated proof uses the correct premise and notes the repair adjacent to the bullet.
- OLP-0047, checking-details.tex: the sentence about the reals says “run through the (tedious) of checking,” omitting the noun after the parenthesis. The translation restores the stated task of checking that the reals form an ordered field and records the repair adjacent to the sentence.
- OLP-0047 later asks the reader to check that subtraction and division of cuts yield cuts, but the corresponding formulas in OLP-0045 are commented out rather than active definitions. The translated paragraph retains the requested checks and explicitly says that they presuppose those intended, inactive formulas.
- OLP-0048, cauchy.tex, has several local prose omissions or duplications: “can be equally be encoded,” “a simple way to see this as follows,” “define we a notion,” and “hone on in.” The translation supplies the minimal grammatical readings without changing any mathematical claim.
- OLP-0048 says to identify real numbers with equivalence relations, where the next sentence correctly defines them as equivalence classes under the relation. Its ordered-field and completeness statements likewise use “Cauchy sequences” as shorthand for their equivalence classes. The translation corrects the first phrase and explicitly states the quotient reading beside the two theorems.
- OLP-0048 compares a purportedly positive real equivalence class with rational zero. The next sentence uses real zero, and the constructed object is in the real quotient, so the translation uses real zero in both places and records the correction adjacent to the definition.
- OLP-0048's completeness proof sketch assumes that an arbitrary upper-bounded real family admits rational constant upper and lower bounds. This is an Archimedean fact about the constructed ordered field, not established in the section before it is used. The translation identifies this dependency adjacent to the step; the subsequent bisection argument is preserved.
- OLP-0058, formulas.tex: the `defIf` branch based on primitive disjunction writes a closing outer parenthesis after not-A-or-B but never opens it. The localized body restores the missing opening parenthesis, yielding the matched formula `(not A or B)`, and identifies the repair as OLPL-001. The exact-source audit is `OLPL001_SOURCE_AUDIT.json`, SHA-256 `1d959ac9106318cc04e7fcace58d3c339aebf975eeee7d32419d198d4a04e16f`; frozen English bytes remain unchanged.
